#ifndef SPLAYSORTNOVO_H
#define SPLAYSORTNOVO_H

#include <vector>
#include <utility> // for std::pair

using namespace std;

// Function prototype
pair<vector<long long>, pair<long long, long long>> splaySort(std::vector<long long> array);

#endif // SPLAYSORTNOVO_H