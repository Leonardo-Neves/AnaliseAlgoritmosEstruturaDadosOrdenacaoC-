#ifndef SPLAYSORT_H
#define SPLAYSORT_H

#include <vector>
#include <utility> // for std::pair

using namespace std;

// Function prototype
vector<long long> splaySort(std::vector<long long> *array, long long *counter_comparisons, long long *counter_movements);

#endif // SPLAYSORT_H